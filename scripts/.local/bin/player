#!/usr/bin/env python

import sys
import subprocess
import urllib.parse

if len(sys.argv) != 2:
    print("Usage: play FILE")
    sys.exit(1)

filename = sys.argv[1].replace('%27', '\'')
subprocess.run(['ffplay', filename])
