source $HOME/.muttpwd

set folder    = "imaps://$my_user@imap.$my_domain:993"
set spoolfile = "+INBOX"

set imap_pass = $my_pass

set smtp_url            = "smtp://$my_user@smtp.$my_domain:587"
set smtp_authenticators = 'login'
set ssl_force_tls       = yes

set smtp_pass = $my_pass
