#!/bin/fish

set id (xdotool search --classname scratch | head -n 1) 

if test -z $id
  bspc rule -a '*:scratch' state=floating
  urxvt -name scratch > /dev/null 2>&1 &
else
  bspc node $id --flag hidden --to-desktop focused -f
end
